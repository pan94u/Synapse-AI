{
  "id": "http-api",
  "name": "HTTP API Server",
  "description": "Generic REST API proxy — 通用 HTTP 请求代理，支持任意外部接口",
  "transport": "stdio",
  "command": "bun",
  "args": ["run", "packages/mcp-servers/src/http-api/index.ts"],
  "env": {
    "HTTP_API_BASE_URL": "${env:HTTP_API_BASE_URL}",
    "HTTP_API_AUTH_HEADER": "${env:HTTP_API_AUTH_HEADER}",
    "HTTP_API_AUTH_VALUE": "${env:HTTP_API_AUTH_VALUE}"
  },
  "enabled": true,
  "autoStart": true,
  "healthCheck": {
    "interval": 30000,
    "timeout": 5000,
    "retries": 3
  },
  "rateLimit": {
    "maxRequests": 60,
    "windowMs": 60000
  },
  "permissions": {
    "tools": ["http_request"],
    "resources": ["*"],
    "requireApproval": ["http_request"]
  },
  "tags": ["http", "rest", "api", "proxy"],
  "category": "infrastructure"
}
